export default function Page(){return <main><h1>arc-dcw-gateway-kit example</h1><p>This example consumes the public core, Circle DCW, and Next adapter packages.</p><p>Set server-only variables from <code>.env.example</code>, then call the wallet and operation endpoints. Without credentials, the API fails closed with a clear configuration error.</p><ul><li><a href="/api/wallet">GET /api/wallet</a> provisions/reuses the example wallet.</li><li>POST /api/wallet/deposit starts/resumes the durable deposit flow.</li><li>POST /api/wallet/withdraw starts/resumes the durable withdrawal flow.</li></ul></main>}
